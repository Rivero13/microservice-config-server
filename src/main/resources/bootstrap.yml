spring:
  application:
    name: config-server
  security:
    user:
      name: jair
      password: jair123
  cloud:
    config:
      server:
        bootstrap: true
        git:
          #uri: https://github.com/Rivero13/servicio-config.git
          uri: https://github.com/Rivero13/microservice-config.git
          search-paths: /
          default-label: "bootcamp1"

server:
  port: 8888

#spring.application.name=config-server
#server.port=8888

#spring.cloud.config.server.git.uri=https://github.com/Rivero13/servicio-config.git

#eureka.client.service-url.defaultZone=http://localhost:8761/eureka
